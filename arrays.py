from array import *
#an array is a contiguous nodes of stored data that are not easily mutable

my_arr = [4,6,7,8,9,2,3, 0]

print(f"Lenght of the array {len(my_arr)}")
print(f"Division of the array {len(my_arr) // 2}")
print(f"Modulos Operator result {len(my_arr) % 2}")

